export const InventoryTab = () => (
  <div style={{ display: 'flex', flexDirection: 'column', flex: 1, alignItems: 'center', justifyContent: 'center', padding: 40, color: 'var(--muted)' }}>
    <div style={{ fontSize: 14, fontWeight: 600, marginBottom: 8 }}>Inventory Levels</div>
    <div style={{ fontSize: 12 }}>Inventory level monitoring will be wired when the inventory tables are migrated.</div>
  </div>
);
