export const PurchaseOrdersTab = () => (
  <div style={{ display: 'flex', flexDirection: 'column', flex: 1, alignItems: 'center', justifyContent: 'center', padding: 40, color: 'var(--muted)' }}>
    <div style={{ fontSize: 14, fontWeight: 600, marginBottom: 8 }}>Purchase Orders</div>
    <div style={{ fontSize: 12 }}>Purchase order tracking will be wired when the PO tables are migrated.</div>
  </div>
);
