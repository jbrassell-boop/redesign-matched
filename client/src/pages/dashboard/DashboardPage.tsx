import { Typography } from 'antd';

const { Title, Text } = Typography;

export const DashboardPage = () => (
  <div style={{ padding: 24 }}>
    <Title level={4} style={{ color: '#1B3A5C', margin: 0 }}>Dashboard</Title>
    <Text type="secondary">Coming in Plan 2.</Text>
  </div>
);
